═══════════════════════════════════════════════════════════════
  🚀 CAMPUSFOUND - PRODUCTION SETUP (ONE COMMAND)
═══════════════════════════════════════════════════════════════

WHAT THIS DOES:
✅ Deletes all test data from MongoDB
✅ Deletes all test images from Cloudinary  
✅ Creates a fresh admin account
✅ Prepares database for production deployment

═══════════════════════════════════════════════════════════════

STEP 1: RUN THE SETUP SCRIPT
────────────────────────────────────────────────────────────────

Open terminal and run:

    cd backend
    npm run setup-production

You will be asked to:
1. Confirm deletion (type "YES")
2. Enter admin name
3. Enter admin email
4. Enter admin password (min 6 characters)

═══════════════════════════════════════════════════════════════

STEP 2: TEST LOCALLY
────────────────────────────────────────────────────────────────

1. Start backend:
   npm run dev

2. Start frontend (new terminal):
   cd ../frontend
   npm run dev

3. Go to: http://localhost:5173/login

4. Login with your admin credentials

5. Verify you can access: http://localhost:5173/admin

═══════════════════════════════════════════════════════════════

STEP 3: DEPLOY TO PRODUCTION
────────────────────────────────────────────────────────────────

1. Push to GitHub:
   git add .
   git commit -m "Production ready with clean database"
   git push origin main

2. Follow DEPLOYMENT_CHECKLIST.md for:
   - Render.com (backend)
   - Vercel (frontend)

═══════════════════════════════════════════════════════════════

ALTERNATIVE: MANUAL STEPS
────────────────────────────────────────────────────────────────

If you prefer to run steps separately:

1. Clean database:
   npm run clean

2. Create admin:
   npm run create-admin

═══════════════════════════════════════════════════════════════

TROUBLESHOOTING
────────────────────────────────────────────────────────────────

❌ "Cannot connect to MongoDB"
   → Check DB_URI in .env file
   → Verify MongoDB Atlas IP whitelist (0.0.0.0/0)

❌ "Cloudinary authentication failed"  
   → Check CLOUDINARY credentials in .env

❌ "User already exists"
   → Run: npm run clean
   → Then: npm run create-admin

═══════════════════════════════════════════════════════════════

IMPORTANT NOTES
────────────────────────────────────────────────────────────────

⚠️  This will PERMANENTLY delete all test data
✅  Your code and .env files are safe
💾  Save admin credentials in password manager
🔒  Never commit .env file to GitHub

═══════════════════════════════════════════════════════════════

READY? Run this command:

    cd backend && npm run setup-production

═══════════════════════════════════════════════════════════════
